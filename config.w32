// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("idebug", "for idebug support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("idebug", "enable idebug support", "no");

if (PHP_IDEBUG != "no") {
	EXTENSION("idebug", "idebug.c");
}

